<apex:page controller="AccountSummaryController">
    <apex:form >
        <apex:pageBlock title="Account">
            <apex:pageBlockTable value="{!accList}" var="item">
                <apex:column value="{!item.name}"/>
                <apex:column value="{!item.Id}"/>
                <apex:column value="{!OppResult}"/>
            </apex:pageBlockTable>
           
            
        </apex:pageBlock>
    </apex:form>
</apex:page>